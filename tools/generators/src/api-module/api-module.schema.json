{
  "$schema": "https://json-schema.org/schema",
  "$id": "ApiModuleGenerator",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "",
      "x-prompt": "What name would you like to use?"
    },
    "createService": {
      "type": "boolean",
      "description": "Create a service for the module",
      "x-prompt": "Do you want to create a service for the module?"
    },
    "createController": {
      "type": "boolean",
      "description": "Create a controller for the module",
      "x-prompt": "Do you want to create a controller for the module?"
    }
  },
  "required": ["name"]
}
